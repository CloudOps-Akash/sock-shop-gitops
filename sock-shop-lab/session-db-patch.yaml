apiVersion: apps/v1
kind: Deployment
metadata:
  name: session-db
spec:
  template:
    spec:
      containers:
        - name: session-db
          volumeMounts:
            - name: redis-data
              mountPath: /data
      volumes:
        - name: redis-data
          emptyDir: {}
